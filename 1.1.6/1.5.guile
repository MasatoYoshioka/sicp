(define (p) (p))

(define (test x y)
  (if (= x 0)
      0
      y))
,trace
(test (p) 0)
;(test 0 (p))

;(p)は自分自身を呼ぶ。なので、(p)を評価すると永久ループになる

;testを評価した時、x = 0 の時 true になり 0 が出力されて終了となると思うが、
;永久ループとなった
;ということは、testが評価されると(if (= x 0))の結果に関わらず、
;y=(p)は評価されている
;なので、わからない

;(test 0 (p)) の時点で評価されているから永久ループなのか
;
;正規順序の場合だと、ifの時に評価されるが、
;作用的順序の場合、まずy=(p)を評価する

;永久ループになった結果を見ると、
;作用的順序での評価をしていると考えられる

